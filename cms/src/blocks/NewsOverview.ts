import { Block } from "payload/types";
import { BackgroundColor } from "../fields/BackgroundColor";

const NewsOverviewBlock: Block = {
  slug: "newsOverviewBlock",
  fields: [BackgroundColor],
};

export { NewsOverviewBlock };
